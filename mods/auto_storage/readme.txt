Adds inventory button to store itemsfrom the players inventory to nearby containers, which already contain at least one of the item.
Also adds sfinv tab for configuring which slots can be auto stored.
